services:
  - type: web
    name: bfhl-api
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/bfhl-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8080
